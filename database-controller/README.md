# Flask Database Controller

## Setting up environment

Create a `.env` file with the following structure

```bash
POSTGRES_USER=db_user
POSTGRES_PASSWORD=magical_password
POSTGRES_DB=plantager_db
```
